import { default as Controller } from '../../scripts/controller.js'

const accessibilityManagerForm = Controller('accessibility-manager-form', (accessibilityManagerForm) => {

})

const utilTitle = Controller('util-title', (utilTitle) => {
    utilTitle.innerHTML="Accessibility Manager Utility"
})