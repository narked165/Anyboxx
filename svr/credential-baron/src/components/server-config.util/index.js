import { default as Controller } from '../../scripts/controller.js'

const serverConfigForm = Controller('server-config-form', (serverConfigForm) => {

})

const utilTitle = Controller('util-title', (utilTitle) => {
    utilTitle.innerHTML="Server Configuration Utility"
})