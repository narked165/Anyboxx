import { default as Controller } from '../../scripts/controller.js'

const serverAdminForm = Controller('server-admin-form', (serverAdminForm) => {

})

const utilTitle = Controller('util-title', (utilTitle) => {
    utilTitle.innerHTML="Server Administration Utility"
})