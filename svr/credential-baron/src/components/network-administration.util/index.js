import { default as Controller } from '../../scripts/controller.js'

const networkAdminForm = Controller('network-admin-form', (networkAdminForm) => {

})

const utilTitle = Controller('util-title', (utilTitle) => {
    utilTitle.innerHTML="Network Admin Utility"
})