import { default as Controller } from '../../scripts/controller.js'

const aboutCredentialBaronForm = Controller('about-credential-baron-form', (aboutCredentialBaronForm) => {

})

const utilTitle = Controller('util-title', (utilTitle) => {
    utilTitle.innerHTML="About Credential Baron Utility"
})