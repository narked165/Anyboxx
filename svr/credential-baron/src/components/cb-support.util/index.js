import { default as Controller } from '../../scripts/controller.js'

const cbSupportForm = Controller('cb-support-form', (networkAdminForm) => {

})

const utilTitle = Controller('util-title', (utilTitle) => {
    utilTitle.innerHTML="Credential Baron Support Utility"
})